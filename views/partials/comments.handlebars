<div class="comments w3-container">
   {{#each this}}
      <section class="comment w3-container">
         <div class="meta">
            {{user.username}}
            on
            {{format_date created_at}}
         </div>
         <div class="text">
            {{comment_text}}
         </div>
      </section>
   {{/each}}
</div>